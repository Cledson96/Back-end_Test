# Base URL for API calls from the browser. In dev, prefer the Vite proxy (relative URLs like `/square-root/...`).
VITE_API_URL="http://localhost:8080"

# Optional: override proxy target when running `vite` (see vite.config.ts)
# VITE_DEV_PROXY_TARGET="http://localhost:8080"
